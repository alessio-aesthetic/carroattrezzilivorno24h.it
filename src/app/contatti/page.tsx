import Link from 'next/link';
import { site } from '@/data/site';

export const metadata = { title: `Contatti | ${site.name}`, description: `Contatta ${site.name} per soccorso stradale e carroattrezzi a Livorno.` };

export default function ContattiPage() {
  return <main className="bg-background-3 dark:bg-background-7 px-4 pb-24 pt-36"><div className="main-container"><div className="mx-auto max-w-[1100px] rounded-[28px] border border-stroke-1 bg-background-2 p-6 sm:p-10 lg:p-16"><div className="grid grid-cols-12 gap-10 lg:gap-20"><div className="col-span-12 lg:col-span-6"><span className="badge badge-green">Parliamo del tuo intervento</span><h1 className="mt-6">Contatta il tuo carroattrezzi a Livorno</h1><p className="mt-6 max-w-[520px]">Spiegaci dove si trova il veicolo, che cosa è successo e dove desideri portarlo. Raccogliere subito queste informazioni ci permette di indirizzare la richiesta in modo ordinato.</p><Link href={`tel:${site.tel}`} className="btn btn-primary btn-lg mt-8">Chiama {site.phone}</Link></div><div className="col-span-12 rounded-[22px] bg-secondary p-7 text-white lg:col-span-6"><h2 className="text-heading-4 font-normal">Informazioni utili</h2><div className="mt-8 space-y-6 text-accent/80"><p><strong className="text-white">Sede operativa</strong><br />{site.address}</p><p><strong className="text-white">Telefono</strong><br /><Link href={`tel:${site.tel}`} className="text-white underline">{site.phone}</Link></p><p><strong className="text-white">Email</strong><br /><a href={`mailto:${site.email}`} className="text-white underline">{site.email}</a></p><p><strong className="text-white">Disponibilità</strong><br />Richieste urgenti durante tutta la giornata, anche in orario serale, notturno e festivo.</p></div></div></div></div></div></main>;
}
