import Link from 'next/link';
import { site, services, zones } from '@/data/site';
import RevealAnimation from '@/components/animation/RevealAnimation';

export default function Footer() { return <footer id="zone" className="bg-secondary relative overflow-hidden"><div className="main-container px-5"><div className="grid grid-cols-12 gap-y-12 py-16 xl:py-[90px]"><div className="col-span-12 xl:col-span-4"><RevealAnimation delay={0.2}><div className="max-w-[340px]"><p className="text-heading-4 font-medium text-white">{site.name}</p><p className="text-tagline-1 mt-4 text-accent/70">Soccorso stradale e carroattrezzi a Livorno, con interventi organizzati in città e nei comuni vicini.</p><p className="mt-6 text-tagline-1 text-accent/70">{site.address}<br />{site.email}<br /><Link href={`tel:${site.tel}`} className="text-white underline">{site.phone}</Link></p></div></RevealAnimation></div><div className="col-span-12 grid grid-cols-12 gap-y-10 xl:col-span-8">{[['Servizi', services.slice(0, 4).map((item) => ({ label: item.title, href: `/servizi/${item.slug}` }))], ['Zone vicine', zones.slice(0, 4).map((item) => ({ label: item.name, href: `/zone/${item.slug}` }))], ['Informazioni', [{ label: 'Contatti', href: '/contatti' }, { label: 'Privacy', href: '/privacy' }, { label: 'Cookie', href: '/cookie' }]]].map(([title, links], index) => <div key={String(title)} className="col-span-12 md:col-span-4"><RevealAnimation delay={0.3 + index * 0.1}><div className="space-y-6"><p className="text-heading-6 text-white">{String(title)}</p><ul className="space-y-3">{(links as { label: string; href: string }[]).map((link) => <li key={link.href}><Link href={link.href} className="footer-link">{link.label}</Link></li>)}</ul></div></RevealAnimation></div>)}</div></div><div className="border-t border-white/15 py-6 text-center"><p className="text-tagline-2 text-accent/60">© {new Date().getFullYear()} {site.name}. Tutti i diritti riservati.</p></div></div></footer>; }
